# Android screencast alias
alias ads="javaws ~/android/androidscreencast.jnlp"

# Git
alias gst='git status'
alias gpl='git smart-pull'
alias gps='git push'
alias gt='git track'
alias gco='git co'
alias gl='git l'
alias glo='git lol'
alias gcm='git cm'
alias gun='git unstage'
alias gts='git staged'
alias gdf='git unstaged'
alias ga='git add . ; git status'
alias gau='git add -u ; git status'

# tig
alias tst='tig status'

# Command Enhancements

# Utility
alias reload='source ~/.zshrc'
alias edit-zsh='subl ~/.zshrc'
alias edit-hosts="sudo subl /etc/hosts"
alias edit-vhosts="sudo subl /Applications/XAMPP/xamppfiles/etc/extra/httpd-vhosts.conf"
alias serve="python -m SimpleHTTPServer"
alias psx="ps ax | grep $1"
alias flushdnscache="sudo killall -HUP mDNSResponder"
alias filetree="ls -R | grep ":$" | sed -e 's/:$//' -e 's/[^-][^\/]*\//--/g' -e 's/^/ /' -e 's/-/|/'"

# Recursively delete `.DS_Store` files
alias cleanup="find . -type f -name '*.DS_Store' -ls -delete"

# Quicker cd
alias ..="cd .."
alias ...="cd ../.."

# Empty the Trash on all mounted volumes and the main HDD
# Also, clear Apple’s System Logs to improve shell startup speed
alias emptytrash="sudo rm -rfv /Volumes/*/.Trashes; sudo rm -rfv ~/.Trash; sudo rm -rfv /private/var/log/asl/*.asl"

# My Code folder
code() {
  cd ~/Projects/
}

# Common -- Some are from Damian Conway
alias ls='ls -G'
alias a='ls -A' # -A all except literal . ..
alias la="ls -A -l -G"
alias c='clear'
alias cdd='cd -'  # goto last dir cd'ed from
alias cl='clear; l'
function cdc() {
    cd $1; ls
}
alias cls='clear; ls'
alias h='history'
alias l.='ls -d .[^.]*'
alias l='ls -lhGt'  # -l long listing, most recent first
                    # -G color
alias lh='ls -lh'
alias ll='ls -lhG'  # -l long listing, human readable, no group info
alias lt='ls -lt' # sort with recently modified first
alias md='mkdir -p'
alias s='cd ..'   # up one dir

function take() {
    mkdir -p "$1"
    cd "$1"
}

alias e='exit'
alias k9="killall -9"
function killnamed () {
	ps ax | grep $1 | cut -d ' ' -f 2 | xargs kill
}

function zipr() {
	zip -r $1.zip $1
}

# Finder
alias o='open . &'

# Processes
alias tu='top -o cpu' # cpu
alias tm='top -o vsize' # memory

# Ruby
alias r="rake"

# Tomcat
alias simulateProd="SPRING_PROFILES_ACTIVE=production"
alias checktomcat="~/.tomcat-check"
alias tomapps="open $CATALINA_HOME/webapps"
alias tomup="sh $CATALINA_HOME/bin/catalina.sh start"
alias tomdown="sh $CATALINA_HOME/bin/catalina.sh stop"
alias tomdebug="sh $CATALINA_HOME/bin/catalina.sh jpda start"
alias tomlog="tail -f $CATALINA_HOME/logs/catalina.out"
alias generateEclipseProject="mvn -o org.apache.maven.plugins:maven-eclipse-plugin:2.6:eclipse"

function canary(){
   open -a "Google Chrome Canary" --args --remote-debugging-port=9222 --user-data-dir="$HOME/.chrome-user-data" $1
}